= simple_form_for(@project) do |f|
  = f.error_notification

  .form-inputs
    = f.input :title
    = f.input :description
    = f.input :image_url
    = f.input :public, hint: 'Projetos privados não são acessíveis pela busca e necessitam da URL para acesso.'

  .form-actions
    = f.button :submit
