%h1 Causas cadastradas

.row
  - @projects.each do |project|
    .col.s6
      .card
        .card-content
          .card-title
            = link_to image_tag(project.image_url, class: 'col s12'), project
            = project.title
          .card-description= project.description

%br

= link_to 'New Project', new_project_path
